import React from "react";

const StudentLookupPage: React.FC = () => (
  <div className="p-4" data-testid="student-lookup">
    <h1 className="text-2xl font-bold">Student Lookup</h1>
    <p>Search for students here.</p>
  </div>
);

export default StudentLookupPage;
